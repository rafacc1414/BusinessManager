{
    "version": "2.0.0",
    "configurations": [
        {
            "name": "Debug",
            "type": "cppdbg",
            "stopAtEntry": true,
            "request": "launch",
            "program": "${workspaceFolder}/build_conan/BusinessManager.bin",
            "cwd": "${workspaceRoot}",
            "MIMode": "gdb"
        }
    ]
}